# Milestones - UI Layout Fix & Backward Movement

Each milestone keeps the project in a working state.

## Milestone 1: Refactor UI Scene Layout
- Edit `res://level/map_viewer_3d.tscn`.
- Add `BackwardButton` (Down button).
- Apply bottom-left and bottom-right anchor presets to the UI elements.

## Milestone 2: Update PlatformController
- Edit `res://level/platform_controller.gd`.
- Wire `BackwardButton` and support backward movement input.

## Milestone 3: Verification
- Verify syntax and run tests.
