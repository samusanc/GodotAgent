Read .AI/MILESTONES.md. Generate a testing strategy for each milestone and save it to .AI/TESTS.md. You must strictly categorize the tests for each milestone into two types:

AI-Friendly Tests: Tests that can be executed via the terminal, scripts, or debuggers where you (the AI) can read the standard output (e.g., unit tests, curl API calls, script outputs, input testing (you can simulate them)).

Human-Only Tests: Tests requiring visual, physical, or manual intervention (e.g., UI visual checks, gameplay mechanics, audio checks).
